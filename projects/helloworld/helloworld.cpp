#include <iostream>
int main(){
	std::cout << "hello world" << std::endl;
	std::cout << "my first vi" << std::endl;
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* shift : w = save */
	/* shift : q = quit */
	/* shift : q = quit */
	// hi this is new code
	// (number)dd = del number of lines from current lines 'only dd deletes the current line'
	// , esc u = undo, (number) yy = copy number of lines' yy only copys current line', 
	// p = paste on current line
	//x = delete current character
	//
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	//
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	// hi this is new code
	// (number)dd = del number of lines from current lines 'only dd deletes the current line'
	// , esc u = undo, (number) yy = copy number of lines' yy only copys current line', 
	// p = paste on current line
	//x = delete current character
	//
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	//
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* l = left, h = right, k = up, j = down, i = insert same line, o = insert next line */
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	/* shift : w = save */
	/* shift : q = quit */
	// hi this is new code
	//
	//
	//
}


